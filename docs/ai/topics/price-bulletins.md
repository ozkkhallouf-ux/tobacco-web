# تقرير موضوع نشرات الأسعار

آخر تحديث: 2026-08-23

## الحالة الحالية

آخر حالة موثقة في `AI_HANDOFF.md`: PR #100 نُشر وحوّل أرقام نشرات الليرة إلى الإنجليزية؛ سبقته إصلاحات القالب المشترك، سعر الصرف، الثيم الفاتح/الداكن، ومنع كاش PDF القديم. التحقق المسجل كان `mismatches=0` و`missing=0` مع سعر صرف 13,100 بتاريخ المهمة؛ هذا رقم تاريخي وليس وعداً بالسعر الحي الحالي.

## المصدر الموثوق

الأسعار المعتمدة في Supabase، وسعر الصرف المعتمد في `scripts/exchange-rate.json`. المخرجات المنشورة تُولّد من الكود الحالي ولا تُعدّل يدوياً.

## نطاق الملفات

`src/app.js`, `src/price-list-template.js`, `scripts/generate-price-lists.mjs`, `scripts/generate-pdfs.mjs`, `scripts/exchange-rate.json`, `scripts/price-data.json`, `public/downloads/`, `.github/workflows/generate-price-lists.yml`, `tools/verify-prices.ps1`.

## قيود ثابتة

- الجملة بالدولار تحتاج وحدة ثانية كاملة؛ المفرق السوري يقبل مخزوناً موجباً وسعره مستقل.
- المعاينة والتوليد المنشور يستخدمان القالب نفسه والثيم المطلوب.
- اعتماد السعر الجديد يجب أن يسبق المعاينة والنشر، ولا يجوز الرجوع لقيمة مخبأة.
- أسماء روابط السوري ثابتة، وتغيير المحتوى يحتاج بصمة PDF وكاش PWA صحيحين.

## فحوص إلزامية

`npm.cmd run check`، توليد HTML/PDF، `tools/verify-prices.ps1` عند وجود بيانات الاتصال المصرح بها، وفحص بصري لكل من العام/الوزاري والفاتح/الداكن عند تغير التصميم.

## الخطوة التالية

أي تعديل لاحق يحدّث هنا سعر الصرف الذي تم اعتماده فعلياً، نتيجة مطابقة الأسعار، وروابط/إصدار النشر الحي.
