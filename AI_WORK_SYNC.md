# مزامنة عمل Claude وCodex — OZK TOBACCO

هذا الملف هو المرجع المشترك لأي مساعد ذكاء اصطناعي يعمل على المشروع. عند اختلاف ذاكرة جلسة قديمة مع هذا الملف أو مع الكود الحالي، تكون الأولوية للكود الحالي ثم لهذا الملف ثم لذاكرة الجلسة.

## قبل بدء أي عمل

1. اقرأ `AGENTS.md` و`CLAUDE.md` وهذا الملف.
2. اقرأ `AI_ACTIVE_TASK.json` و`AI_HANDOFF.md` لمعرفة مَن يعمل وأين انتهى.
3. اقرأ `docs/ai/README.md` ثم تقرير الموضوع المناسب تحت `docs/ai/topics/`، واستعمل `docs/ai/impact-map.md` لتحديد الملفات والفحوص المرتبطة.
4. تأكد أن مجلد العمل هو مستودع `tobacco-web` الحقيقي، فهو المصدر الرسمي الوحيد للكود؛ المشاريع أو المحادثات الأخرى مراجع وليست نسخة تشغيل ثانية.
5. افحص حالة Git. لا تمسح تغييرات موجودة ولا تخلطها مع مهمة أخرى.
6. إذا كان المجلد نظيفاً، نفّذ `git pull --rebase origin main` قبل التعديل.
7. راجع آخر التعديلات ذات الصلة في `git log` ولا تعتمد على محادثة قديمة وحدها.
8. احجز المهمة على `main` قبل إنشاء فرع العمل: `powershell -File tools/ai-work-coordination.ps1 -Action Claim -Owner Codex -Task "وصف المهمة" -Files src/app.js`.
9. انشر الحجز فوراً بـcommit وpush عاديين على `main` كي تراه الجلسات والأجهزة الأخرى، ثم أنشئ فرعاً منفصلاً للمهمة.

إذا كان القفل `active` فلا تبدأ عملاً متداخلاً. يجوز العمل بالتوازي فقط عندما تكون الملفات والنطاقات منفصلة بوضوح، وبعد تسجيل ذلك في دفتر التسليم.

## أثناء العمل

- العربية هي اللغة الرسمية للواجهة والتواصل، مع RTL ونص واضح على iPhone.
- Windows هو بيئة التطوير والمزامنة، وiPhone هو جهاز الاستخدام الأساسي.
- حافظ على بنية HTML/JavaScript/CSS الحالية؛ لا تدخل React أو Vite من دون طلب صريح. macOS هدف مساعد اختياري بقرار المالك (2026-08-31) محصور بمرافق OZK المحلي وأرشفة iCloud، ومُبوَّب فلا يمسّ iPhone أو Windows.
- الأمين هو المصدر المحاسبي الموثوق. لا تصحح رقماً يدوياً قبل فحص المصدر والمزامنة.
- لا تقرأ أو تنسخ `.env` أو مفاتيح أو كلمات مرور أو connection strings.
- لا تنشر تقارير أو أرصدة أو بيانات زبائن على استضافة عامة.
- فواتير المشتريات: التطوير (واجهة، جدول Supabase، سكريبتات المزامنة) مسموح على فروع منفصلة، لكن **تفعيل أي كتابة فعلية على الأمين ممنوع** حتى إذن صريح لاحق — انظر سجل القرارات 2026-07-29 أدناه لتفاصيل الحالة.
- إذا اكتشفت قاعدة محاسبية أو سبب عطل جديد، وثّقه في قسم سجل القرارات أدناه باختصار مع التاريخ والملفات المتأثرة.
- بعد كل تغيير جوهري، حدّث تقرير الموضوع المناسب في `docs/ai/topics/` بالحالة المثبتة والفحوص والملفات والخطوة التالية؛ لا تنسخ أسراراً أو بيانات زبائن إلى التقرير.
- استخدم فرعاً منفصلاً لكل إصلاح أو ميزة. لا يعمل مساعدان على الفرع نفسه.

## قبل إعلان النجاح

1. شغّل `npm.cmd run check` على Windows.
2. اختبر الحسابات بمقارنة مستقلة مع مصدر الأمين عند تعديل المخزون أو الأرصدة أو الفواتير.
3. لا ترفع `CACHE_NAME` يدوياً عند تعديل HTML أو CSS أو JavaScript — `pages.yml` يرفعه ومعامل `?v=tobacco-N` آلياً في كل نشر. أما أصل **جديد** في `index.html` فيحتاج `?v=tobacco-N` مرة واحدة يدوياً.
4. اعرض `git diff --check` وراجع أن التغيير يخص المهمة فقط.
5. تأكد أن تقرير الموضوع يصف النتيجة الفعلية الحالية، لا الخطة القديمة.
6. لا تدّع نجاح النشر أو iPhone من دون دليل فعلي.
7. عند اكتمال المهمة ونجاح فحوصها، احفظها فوراً بـcommit على فرع مهمتها وادفع الفرع إلى GitHub كنسخة احتياطية — لا يُترك عمل مكتمل بلا حفظ أبداً. الدمج في `main` والنشر الفعلي يبقيان بطلب صريح من المستخدم.
8. بعد دمج فرع المهمة، ارجع إلى `main` واسحب آخر نسخة، ثم أغلق القفل وحدّث التسليم: `powershell -File tools/ai-work-coordination.ps1 -Action Complete -Owner Codex -Task "وصف المهمة" -Note "النتيجة والفحوص"`.
9. انشر تحديث التسليم وإخلاء القفل فوراً بـcommit وpush عاديين على `main`.

قفل المهمة وتسليمها هما الاستثناء التنظيمي الوحيد الذي يُنشر مباشرة على `main`؛ كود الموقع والإصلاحات والميزات تبقى دائماً على فروع مستقلة وتُدمج بعد الفحص.

## قواعد محاسبية لا يجوز كسرها

- المخزون يُحسب من حركات الفواتير وأعلام `bIsInput` و`bIsOutput`، لا من `ms000` بعد تدوير السنة.
- كشف الحساب يستعمل `balance` بترتيب كشف الأمين، بينما مستند الفاتورة يستعمل `balanceChrono` و`docPrev`/`docNew` للسند الكامل.
- أساس سعر سطر الفاتورة قد يكون كرتونة أو كروز ويُحسم عبر `invoicePriceBasis` بمطابقة الإجمالي.
- **الحسم ودفعة الزبون عمليتان محاسبيتان مستقلتان ولا يجوز دمجهما في مستند الفاتورة.** الرصيد الجديد = الرصيد السابق + قيمة الفاتورة − الحسم − دفعة الزبون، ولكلٍّ سطره الخاص يظهر فقط إن وُجد. مسار فاتورة المبيعات من الموقع يفصلهما أصلاً (`salesTotals`: `state.salesDiscount` مقابل `state.salesPaid`). أما مستند فاتورة الأمين فكان يطبع الفرق `prevBalance + total − newBalance` بعنوان «حسم» بلا شرط، فيبتلع أي دفعة على نفس السند ويطبعها حسماً — خطأ محاسبي في ورقة تُسلَّم للزبون. **فجوة بيانات قائمة:** `tools/push-customer-invoices.ps1` يجلب الفاتورة بحقول `number/date/guid/total/isReturn/lines` فقط، بلا حسم رأس الفاتورة وبلا الدفعة المرافقة؛ فحتى تُجلبا يبقى الفرق «تسوية على الحساب» غير منسوبة ولا يُسمّى حسماً أبداً. جلب الحقلين يحتاج اكتشافاً قرائياً لمخطط الأمين على جهاز Windows — لا يُخمَّن اسم عمود.
- مرتجع المبيعات هو `BillType=3` وقد يحتاج مطابقة بالتاريخ والمبلغ عند غياب `BiGUID`.
- عند رصيد قديم، افحص توقيت مهمة المزامنة وقارن `customerAccountGuid` مع `ac000` قبل تغيير المنطق.
- نشرة الجملة بالدولار تعرض الصنف فقط إذا كان مخزونه يساوي وحدة ثانية كاملة واحدة على الأقل (`stock_qty / unit2_factor >= 1`). نشرة المفرق بالسوري تقبل أي مخزون موجب، ويجوز تسعيرها بلا سعر دولار.
- سعر المفرق (`price_payload.retail.price`) مستقل تماماً عن سعر الجملة: يجوز حفظه عندما يكون `unit2_price = 0`، وتستعمل نشرة السوري هذا السعر، بينما يبقى الصنف خارج نشرة الدولار حتى يُسعّر للجملة وتتوافر وحدة ثانية كاملة.
- سعر صرف السوري اليومي محفوظ في `scripts/exchange-rate.json`. حقل «سعر الصرف اليوم» داخل مركز النشرة يرسله إلى workflow عند «اعتماد ونشر»، فيُحدّث الملف وتستخدمه التوليدات اليدوية والمجدولة اللاحقة؛ أسماء روابط ملفات السوري تبقى ثابتة للتوافق.
- عند حفظ سعر من الموقع، يجب تحديث كل مفاتيح الاسم المكررة بعد التطبيع (همزة/تاء مربوطة/ترقيم) بالقيمة نفسها، ثم جدولة نشر النشرة تلقائياً بعد 15 ثانية من آخر تعديل إذا كان رمز النشر محفوظاً. المولّد يقرأ المصدر بلا cache ويختار أحدث سجل، مع تفضيل مفتاح الأمين المطابق عند تعادل الوقت.
- إلى أن تتوحّد aliases القديمة في Supabase، تعارض «ماستر كوين أبيض» يُحسم إلى 340$ وتعارض «1970 كوين أبيض» إلى 275$ في نشرة الجملة. الحسم يعمل فقط عند وجود قيم متعارضة ويتوقف تلقائياً بعد توحيدها بالحفظ من الموقع.
- قائمة «أسعار النشرة» داخل الموقع تطابق النشرة العامة: تستبعد الوزاري، تعرض الدمج نفسه للمعسل والأصناف المتشابهة، وتطبّق شرط الوحدة الثانية الكاملة في وضع الجملة فقط. حفظ سعر سطر مدمج يحدّث جميع أصنافه الأصلية.
- مولّد النشرات يجب أن يدفع ملفات الأسعار من دون `[skip ci]` كي يشغّل `pages.yml` وينشر الأسعار الجديدة فعلياً؛ مسار توليد النشرة محصور بتغيير `scripts/price-data.json` فلا يسبب حلقة تشغيل عند دفع ملفات `public/downloads`.
- مزامنة مخزون النشرة تقارن العدد الصحيح للوحدة الثانية الكاملة (`floor(stock_qty / unit2_factor)`) بين الأمين والمخزون المنشور، وتحدّث عند تغيّره أو عند الانتقال بين مخزون موجب وصفر؛ لذلك ينطبق الحد تلقائياً على الكرتونة والطرد والشرحة حسب معامل كل صنف.
- تقرير المخزون يعرض كل صنف ذي كمية موجبة مستقلاً بلا دمج (بما فيها أصناف المعسل)، ويعرض النافد فقط إذا كان له مبيع فعلي حديث. تصنيف «قريب من النفاد» يعتمد تغطية آخر 30 يوماً من حركة المبيع، مع إبقاء حدود الأمان الخاصة للماستر والغلواز؛ الصنف البطيء بلا مبيع حديث لا يُصنّف قريب النفاد لمجرد أن كميته دون حد ثابت.

## سجل القرارات المشتركة

- 2026-09-05 (هوية الزبون بين تقارير الأمين — فواتير مفقودة من كشف الحساب وزر PDF): تقرير `ameen_customer_invoices` كان يجمّع الفواتير بـ`bu000.Cust_Name`، وهي **لقطة نصّية على رأس كل فاتورة**، بينما يجمّع تقريرا الأرصدة والحركات بـ`cu000.CustomerName` (اسم الحساب). الحقلان مصدران مختلفان، فأي إعادة تسمية حساب في الأمين تفكّ الربط. مقيس على الإنتاج: أُعيدت تسمية حساب الساعة 10:26 فحملت الأرصدة (مزامنة كل دقيقة) الاسم الجديد فوراً وبقيت الفواتير (مزامنة كل ساعة) على القديم حتى 10:53 — فظهرت «لا توجد فواتير لهذا الزبون» في كشف الحساب وفشل زر «فاتورة PDF» بينما الفاتورة ظاهرة في قائمة الفواتير (تمرّ على كل المجموعات بلا مطابقة اسم). والأسوأ أنه انشطار **دائم** لا نافذة عابرة: بعد المزامنة بقيت فاتورة قديمة تحت الاسم السابق، فبقي كشف الحساب ناقصاً فاتورة واحدة. **القاعدة المعتمدة: `customerGuid` هو الهوية، والاسم احتياط للتقارير القديمة فقط.** أُضيف `customerGuid`/`customerAccountGuid` إلى مخرجات `tools/push-customer-invoices.ps1` (باكتشاف اسم العمود على `bu000` كما تُكتشف بقية الأعمدة، وسقوط آمن للاسم إن غاب) و`customerGuid` إلى `tools/push-customer-movements.ps1`؛ وفي `src/app.js` صارت `customerInvoicesFor` تقبل عنصر الزبون لا اسمه فقط، وتُرجع **اتحاد** مجموعته بالهوية مع أي مجموعة يتيمة (اسم بلا حساب قائم) يثبت دفتر حسابه ملكيتها — بلا تبنٍّ إطلاقاً عند وجود أكثر من مرشّح. `invoiceByGuid` تبحث عن الفاتورة بمعرّفها على كامل التقرير فلا يعود زر PDF معتمداً على اسم الزبون. قياسان إضافيان: (1) **كل** الـ1830 قيداً في تقرير الحركات تحمل `billGuid` صفرياً، فالربط «القطعي» بالمعرّف لم يكن يعمل أصلاً وكان `movementForBill` سيُرجع أول قيد للزبون أياً كان — عولج بـ`normGuid` التي تعتبر الصفري غياباً؛ (2) تقرير الفواتير رُفع 7 مرات فقط في 24 ساعة بمتوسط فجوة 235 دقيقة رغم جدولة الساعة، فخُفّض التواتر إلى 15 دقيقة وأُضيف `-StartWhenAvailable` كي تُعوَّض التشغيلات الفائتة. أخيراً: الخطأ لم يكن صامتاً في الكود بل غير مرئي على الشاشة (لوحة الرسائل أعلى `<main>` والزر بعد مئات البكسلات) — أُضيفت `showNoticeNow` التي تُظهر اللوحة فعلياً، ورسائل الغياب صارت تميّز «لم تصل المزامنة» و«المزامنة أقدم من الدفتر بـN دقيقة» و«لا فواتير خلال النافذة». الفحص: `scripts/check-customer-invoice-identity.mjs` (18 اختباراً؛ يفشل على السلوك القديم).   ⚠️ **تصحيح لاحق بنفس اليوم بعد ملاحظة P1 من Codex على PR #189:** كانت النسخة الأولى تنسب المجموعة «اليتيمة» (اسم بلا حساب قائم) إلى الزبون الذي يطابق دفترُ حسابه فواتيرَها تاريخاً ومبلغاً وجهةً، بحارس «مرشّح واحد فقط». الملاحظة صحيحة وأُثبتت بالقياس على الإنتاج: **12 من 864 زوج (تاريخ، مبلغ مدين) يتشاركه أكثر من زبون — 1.4%**، ومجموعة بفاتورة واحدة تُطابَق بزوج واحد. فيكفي أن يغيب دفتر المالك الحقيقي عن اللقطة (تأخّر مزامنة الحركات ~25 دقيقة، أو اقتطاع `MaxMovementsPerCustomer = 300`) ليصير زبون آخر «المرشّح الوحيد» فتُنسب إليه فاتورة ليست له — بأصنافها وأسعارها — وتُصدَّر PDF باسمه. **أُزيل الاستدلال بالكامل: لا نسبة بلا معرّف.** والبديل ليس الصمت: `invoiceIdentityWarning()` تُظهر تحذيراً يسمّي المجموعة اليتيمة بلا نسبتها لأحد، ويختفي فور وصول تقرير يحمل المعرّف. القاعدة المستخلصة: **إصلاح هوية البيانات مكانه المصدر (تجميع المزامنة بالمعرّف) لا الواجهة؛ والاستدلال على الملكية في مستند محاسبي يُسلَّم للزبون ممنوع مهما بدا حارسه محكماً.**

  **الحالة: على فرع `claude/tobacco-invoice-bug-9e3686`، غير مدموج. تعديلا PowerShell لم يُشغَّلا (لا وصول لقاعدة الأمين من macOS) — شغّل `-Discover` أولاً على Windows وتحقق بالعين من سطر «هوية الحساب» قبل أول تشغيل فعلي.**

- 2026-08-31 (أرشفة تلقائية إلى iCloud على الماك): أُضيف جسر محلي اختياري `tools/mac-archive-bridge/` يستمع على `127.0.0.1:8787` فقط، يستقبل من الموقع نوع المستند + HTML (أو PDF جاهز) وبيانات وصفية، ويحفظ نسخة PDF داخل مجلد iCloud Drive المناسب باسم عربي حسب الاصطلاح المعتمد. **الموقع لا يرسل مساراً ولا اسم ملف؛ الجسر وحده يقرر.** الوصلة في `src/app.js` عبر `archiveToICloud()` ووسيط `archive` في `exportReportPdf`/`printHtmlDocument` — تُطلق بلا `await` وتبتلع كل خطأ، فالأرشفة **ميزة مساعدة منفصلة تماماً عن العملية التجارية** ولا تُوقف تصديراً ولا بيعاً. لم يُمسّ منطق محاسبي ولا تكامل الأمين (يبقى READ ONLY كما هو). ثلاث قواعد مثبتة بالقياس على جهاز الماك: (1) أسماء مجلدات iCloud ليست بصيغة Unicode واحدة — `فواتير الزبائن` و`نشرات أسعار` مخزَّنان NFD و`فواتير المشتريات` يبدأ بمحرف U+200F مخفي، فالمطابقة تتم بتطبيع Unicode والكتابة بالاسم الحقيقي من `readdir`؛ (2) `WorkingDirectory` لـLaunchAgent داخل `~/Documents` يجمّد Node عند `getcwd` فلا يستمع إطلاقاً — يجب أن يكون خارجها؛ (3) بصمة منع التكرار يجب أن تتجاهل `CreationDate`/`ModDate` وإلا أنتج تصديران متتاليان لنفس المستند نسخة «(2)» زائدة. أُضيف `scripts/check-mac-archive-bridge.mjs` (44 فحصاً سلوكياً على جذر iCloud وهمي) إلى `npm run check`، ووُسّع `connect-src` بالمنفذ الصريح للجسر فقط بلا أي wildcard. قاعدة رابعة مقيسة: **من صفحة `https` لا يكفي أن يعمل الجسر — يلزم إذن «الوصول للشبكة المحلية»**؛ الطلب فشل بلا إذن ونجح بـ200 فور منحه، بينما يعمل مباشرة من `http://localhost:5173`. بناء WebKit (محرك Safari و`OZK.app`) حجب الطلب في الحالتين فيبقى التحقق على Safari الفعلي مطلوباً، والتوصية الحالية على الماك: Chrome أو الخادم المحلي. **الحالة: على فرع `claude/mac-icloud-archive-bridge`، غير مدموجة في `main` ولم تُنشر — الميزة تخص الماك وحده ولا تغيّر شيئاً على Windows أو iPhone.** قرارات المالك 2026-08-31 بعد المراجعة: (أ) فاتورة المرتجع نوع مستقل `return_invoice` يُحفظ في «فواتير الزبائن» باسم «فاتورة مرتجع - {الزبون} - رقم {الرقم} - {التاريخ}.pdf» ولا يُخلط مع `invoice` داخلياً؛ (ب) `connect-src` يسمح بأصل محلي واحد بالضبط `http://127.0.0.1:8787` بلا wildcard ولا `localhost` ولا منفذ ثانٍ. تشخيص 2026-08-31 بعد اختبار المالك على `OZK.app`: **السبب الجذري لعدم ظهور أي أرشفة هو أن الموقع المنشور لا يحوي الميزة أصلاً** (`v=tobacco-172`، بلا `icloud-archive.js`) — لا علاقة لـSafari بالأمر. وبقياس مستقل على Safari الفعلي (مستوى TCP): صفحة `http` محلية لا تعمل في Safari إطلاقاً لأنه يحاول مصافحة TLS لكل مورد فرعي، وتكرّر ذلك بعد إزالة `upgrade-insecure-requests` فالسبب سلوك Safari لا سياسة الموقع. **القرار: متصفح الماك المعتمد هو Chrome، والموقع يُفتح من مرافق محلي `com.ozk.local-site` على `127.0.0.1:5173`** فتعمل الأرشفة بلا أي إذن شبكة محلية وبلا اعتماد على النشر. `scripts/serve.mjs` صار يقبل `HOST`/`PORT` مع إبقاء الافتراضي `0.0.0.0` — **ويندوز لم يتغيّر سلوكه إطلاقاً**، والميزة محصورة بالماك داخل الكود عبر `isSupportedPlatform()`. التفاصيل: `docs/ai/topics/icloud-archive.md`.

- 2026-07-29 (تطوير فواتير المشتريات — الأمين): بُنيت نسخة v2 كاملة لتبويب «فواتير المشتريات» على فرع `worktree-purchase-invoices-ameen-v2` منفصل عن `claude/purchase-invoices-tab-367d1b` و`claude/purchase-invoices-statement-cc9f08` (لم تُلمس): بحث مورد+صنف بوحدات أمين حقيقية، لوحة معلومات صنف (مخزون من حركات الفواتير لا `ms000`، آخر سعر/تاريخ/مورد، متوسط تكلفة بمصدره وعملته)، أصناف مقترحة حسب حركة 30 يوماً، عملة USD/SYP صريحة بلا تحويل ضمني، تبديل نقدي/آجل مع تسجيل دفعة جزئية والمتبقي محسوباً دوماً، دورة حياة 5 حالات (`draft→approved→sync_pending/failed→synced`) عبر `poCalc.poCanTransitionStatus`، وفاتورة `synced` لا تُحذف ولا تُعدَّل إلا بـ«إجراء تصحيحي» موثّق (`correction_count`/`correction_log`). أُضيف ملف حسابات صِرف بلا DOM `src/purchase-invoice-calc.js` (يُختبر مباشرة عبر `vm` في `scripts/check.mjs`)، وجدول Supabase جديد `supabase/purchase-invoices-ameen-sync.sql` (لم يُطبَّق، ولم يُلمس `supabase/purchase-invoices-table.sql` القديم)، وثلاث سكريبتات PowerShell جديدة تحت `tools/` (`discover-ameen-purchase-schema.ps1` قراءة فقط، و`push-purchase-item-snapshot.ps1`/`sync-purchase-invoices-to-ameen.ps1` كتابة) — **الثلاثة مقفلة بـ`exit 1` غير مشروط ولم تُشغَّل ولو بوضع تجربة (DryRun)**. لا BillType/GUID/حساب/صندوق أميني حقيقي مفترَض أو مكتوب بالكود؛ يجب اكتشافه فعلياً عبر `discover-ameen-purchase-schema.ps1` (قراءة فقط) قبل أي تفعيل. **الحالة: تطوير/تجهيز فقط — لا تفعيل، لا تشغيل مزامنة فعلية، بانتظار إذن صريح لاحق لفك القفل وتطبيق SQL الجديد.**
- 2026-07-29: تأكد بفحص SQL قرائي على خادم الأمين أن `AmnDb002` هي القاعدة النشطة (402 مادة وكتابة حديثة تطابق عدد مواد المزامنة)، بينما `AmnDb001` قديمة/مؤرشفة (391 مادة وبلا كتابة حديثة). يجب أن تستخدم إعدادات التهيئة والتوثيق والاستعلامات `AmnDb002` افتراضياً؛ لا تعِد توجيه المزامنة إلى `AmnDb001` ولا تحاول تنشيط نسخها الاحتياطية بوصفها قاعدة تشغيلية.
- 2026-07-23: بطاقتا أمين متطابقتان بعد تطبيع الاسم (مثل 273/274 — فرق نقطة آخر الاسم فقط) تسبب تذبذباً حياً بمخزون النشرة (الكتابة تتناوب بين قيمتيهما كل تشغيلة مزامنة). `Sync-PriceListStockOnFullUnitChange` في `tools/ameen-sync-agent.ps1` يجب أن يجمع أصناف التقرير حسب المفتاح المطبّع قبل المقارنة (لا مقارنة صنف بصنف)، و`tools/pull-item-numbers.ps1` يجب أن يكشف تصادمات التطبيع ويرفض أي تصادم غير محسوم صراحة بدل كتابة رقم عشوائي. أي حسم صريح لتصادم يجب أن يتحقق أن الرقم المحسوم لا يزال أحد المرشحين الفعليين قبل تطبيقه (`claude/sales-invoice-normalization-issue-30bbb3`).
- 2026-07-22 (مساءً): إصلاح نطاق service worker — ملف جذري `service-worker.js` يستورد `public/service-worker.js` (آلية رفع CACHE_NAME في الـ workflow بقيت كما هي)، فيفتح التطبيق من الكاش عند توقف السيرفر المحلي؛ التسجيل القديم بنطاق public/ يُلغى تلقائياً من app.js. تحصين Supabase: سُحب تنفيذ 8 دوال حساسة (send_*_report, dispatch_*, prune_inventory_reports, auto_confirm_new_user, rls_auto_enable) من public/anon/authenticated — تعمل عبر pg_cron والخدمة فقط؛ قُصرت واجهات SECURITY DEFINER على SELECT؛ ثُبّت search_path لدالتين؛ فهارس FK جديدة وحذف قيد مكرر وتحويل سياسات إلى `(select auth.role())`. **قاعدة حرجة:** `approved_price_sync_feed` و`available_price_sync_feed` يجب أن تبقيا SELECT لدور anon — يقرؤهما `pull-approved-prices.ps1` ومولّد النشرات في GitHub Actions بالمفتاح العام (سحبُها جُرِّب فكسر السحب وأُعيد فوراً). `notify_telegram` تتطلب authenticated (سكريبتات ويندوز). أُضيف سحب نسخ الأمين الاحتياطية إلى OneDrive: `tools/pull-ameen-backup.ps1` يومياً 23:00 عبر مشاركة `\\OZK-TOBACCO\AmeenBackup` (ينسحب بصمت حتى تفعيل المشاركة على جهاز الخادم).
- 2026-07-22: تعديل قاعدة الحفظ باتفاق صريح مع المستخدم: المهمة المكتملة والمفحوصة تُحفظ فوراً بـcommit على فرعها ويُدفع الفرع إلى GitHub احتياطياً؛ الدمج في `main` والنشر يبقيان بطلب المستخدم. السبب: أعمال 15–21 تموز المكتملة بقيت أسبوعاً بلا أي حفظ وكادت تضيع (أُنقذت بالكوميت 686ff05). أُضيفت أيضاً مهمتا Windows: «TOBACCO Local Web Server» (حارس سيرفر localhost:5173 كل 5 دقائق) و«TOBACCO Daily Git Pull» (سحب يومي 07:30 عند نظافة المستودع فقط) (`tools/ensure-local-server.ps1`, `tools/daily-git-pull.ps1`).
- 2026-07-21: تقرير دفعات وصناديق تيليغرام يقرأ `en000` فقط من الأمين؛ المدين على الصندوق وارد والدائن صادر، والمبلغ الأصلي = مبلغ الأساس بالدولار ÷ `CurrencyVal`. تعرض الصناديق 133/134/137/138 كلٌ بعملته، وتُفصل المناقلات التي يكون `ContraAccGUID` فيها تحت جذر الأموال الجاهزة 13 حتى لا تتكرر ضمن الوارد والصادر (`tools/push-daily-movement.ps1`, `supabase/functions/telegram-webhook/index.ts`).
- 2026-07-18: نموذج الفاتورة لا يعيد رسم الصفحة أثناء كتابة اسم الزبون كي يبقى التركيز ثابتاً، وحقلا الكمية والسعر يستخدمان إدخالاً عشرياً باتجاه يسار إلى يمين مع تحويل الأرقام العربية والفارسية إلى أرقام إنجليزية (`src/app.js`, `src/number-normalizer.js`).
- 2026-07-18: تغيير اسمي «كابتن بلاك كوين أزرق/أسود» في الأمين إلى «كابتن بلاك كور أزرق/أسود جديد». تُعامل الأسماء القديمة كـ aliases للاسمين الفعليين الكاملين في مطابقة المخزون وتطبيق الأسعار وفحصها، وتعرض النشرة الاسمين الجديدين (`src/app.js`, `tools/ameen-sync-agent.ps1`, `tools/apply-approved-prices-to-ameen.ps1`, `tools/verify-prices.ps1`, `scripts/generate-price-lists.mjs`).
- 2026-07-16: أرصدة الذمم تُؤخذ من `ac000.Debit - ac000.Credit` عبر `customerAccountGuid` بعملة الأساس (USD)، لا من رصيد `cu000` بعملة حساب الزبون، ولا تُحوّل مرة ثانية حسب إعداد عملة الوصل (`tools/ameen-customer-balances-query.sql`, `src/app.js`).
- 2026-07-14: اعتماد هذا الملف كمرجع تنسيق مشترك بين Claude وCodex لمنع العمل على نسخة قديمة أو تكرار أخطاء محاسبية سابقة.
- 2026-07-15: اعتماد ترتيب مجموعات النشرة في تقرير المخزون، والتصنيف المبني على حركة المبيع، والتصميم الفاتح الثابت للطباعة (`src/app.js`).
- 2026-07-15: تقرير ربح تيليغرام يعتمد قيم عملة الأساس بالدولار من الأمين: مبيعات `BillType=1`، مرتجعات `BillType=3`، تكلفة `bi.UnitCostPrice × bi.Qty`، حسومات وإضافات رأس الفاتورة، ثم مصاريف التشغيل تحت حساب المصاريف الرئيسي مع استثناء نقل المشتريات لمنع احتساب التكلفة مرتين (`tools/push-daily-profit.ps1`).

<!-- اختبار نهائي دورة كاملة تلقائية Codex Review Gate بعد دمج PR #130 - 2026-08-29T12:14:38Z -->

<!-- اختبار نهائي دورة كاملة تلقائية Codex Review Gate بعد دمج PR #130 -->
<!-- push ثانٍ لاختبار رجوع الـGate إلى FAIL بعد push جديد -->
